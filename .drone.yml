kind: pipeline
name: default

steps:
  - name: build
    image: plugins/docker
    settings:
      repo: xmorse/drone-changeset
      username: xmorse
      tags: 
      - latest
      password:
        from_secret: docker_password
